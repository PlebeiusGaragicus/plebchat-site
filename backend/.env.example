## Server Configuration

HOST=0.0.0.0
PORT=8000

# CORS Configuration (comma-separated list of additional origins)
FRONTEND_URL=
ADMIN_FRONTEND_URL=


## Wallet Configuration

# REQUIRED: BIP39 12-word mnemonic for deterministic wallet recovery
# Generate with: python -c "from mnemonic import Mnemonic; print(Mnemonic('english').generate())"
# IMPORTANT: Back this up securely! Without it, you cannot recover funds.
# The server will NOT start without this set.
WALLET_MNEMONIC=


## Mint Configuration

# Primary Cashu mint URL (default: minibits)
CASHU_MINT_URL=https://mint.minibits.cash/Bitcoin

# Additional trusted mints (comma-separated)
# Tokens from mints not in this list will be rejected.
# The primary mint (CASHU_MINT_URL) is always trusted.
# Example: TRUSTED_MINTS=https://mint.example.com,https://other.mint.com
TRUSTED_MINTS=


## Automatic Lightning Payout Configuration

# Lightning address for automatic payouts (e.g., user@getalby.com)
# Leave empty to disable automatic payouts
PAYOUT_LN_ADDRESS=

# Minimum balance (in sats) to trigger automatic payout (default: 1000)
PAYOUT_THRESHOLD_SATS=1000

# Payout check interval in seconds (default: 300 = 5 minutes)
PAYOUT_INTERVAL_SECONDS=300


## Admin Configuration

# REQUIRED: Comma-separated list of admin npubs or hex pubkeys for NIP-98 auth
# Example: ADMIN_NPUBS=npub1abc...,npub1xyz...
ADMIN_NPUBS=


## Development

# Enable debug mode (1 = enabled, 0 = disabled)
DEBUG=0
